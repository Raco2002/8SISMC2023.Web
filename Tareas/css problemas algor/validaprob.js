function validar(problemas) {
    var tasaInteres = 0.02; // Tasa de interés mensual (2%)
    var tiempo = 1; // Número de meses
    var montoGastado = capital * Math.pow(1 + tasaInteres, tiempo); // Cálculo del monto gastado después de un mes
    
    return montoGastado; // Devuelve el monto gastado después de un mes.

        if(montoGastado = 5){
        alert("Ingrese otro numero");
    }
  }